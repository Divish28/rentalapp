import ActionType from "../contents/ActionType";

const initialState={
    houses:[]
}
export const HouseReducer=(state,action)=>{
    switch (type) {
        case ActionType.SET_HOUSES:
            return state;
    
        default:
            return state;
    }

}